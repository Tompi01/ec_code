{% extends 'auth.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <!-- Page -->
    <div class="flex items-center justify-center grow bg-center bg-no-repeat page-bg">
        <div class="card max-w-[370px] w-full">
            <form name="registration_form" class="card-body flex flex-col gap-5 p-10" id="sign_up_form" method="post">
                <div class="text-center mb-2.5">
                    <h3 class="text-lg font-medium text-gray-900 leading-none mb-2.5">
                        Inscription
                    </h3>
                    <div class="flex items-center justify-center">
                        <span class="text-2sm text-gray-700 me-1.5">
                        Vous avez déjà un compte ?
                        </span>
                        <a class="text-2sm link" href="{{ path('auth.login') }}">
                            Me connecter
                        </a>
                    </div>
                </div>
                <div class="flex flex-col gap-1">
                    {{ form_errors(registrationForm.email) }}
                    <label class="form-label text-gray-900">Email</label>
                    <input class="input" name="registration_form[email]" placeholder="Adresse email" type="text" value=""/>
                </div>
                <div class="flex flex-col gap-1">
                    {{ form_errors(registrationForm.plainPassword) }}
                    <label class="form-label font-normal text-gray-900">Mot de passe</label>
                    <div class="input" data-toggle-password="true">
                        <input name="registration_form[plainPassword]" placeholder="Mot de passe" type="password" value="">
                        <button class="btn btn-icon" data-toggle-password-trigger="true" type="button">
                            <i class="ki-filled ki-eye text-gray-500 toggle-password-active:hidden">
                            </i>
                            <i class="ki-filled ki-eye-slash text-gray-500 hidden toggle-password-active:block">
                            </i>
                        </button>
                        </input>
                    </div>
                </div>
                <label class="checkbox-group">
                    <input class="checkbox checkbox-sm" name="registration_form[Condition]" type="checkbox" value="1"/>
                    <span class="checkbox-label">
                        J'accepte les <a class="text-2sm link" href="#">GCU</a>
                    </span>
                    {{ form_errors(registrationForm.Condition) }}
                </label>
                <button type="submit" class="btn btn-primary flex justify-center grow">Inscription</button>
                <input type="hidden" id="registration_form__token" name="registration_form[_token]" data-controller="csrf-protection" autocomplete="off" value="csrf-token">
                </form>
            </div>
    </div>
    <!-- End of Page -->
{% endblock %}
